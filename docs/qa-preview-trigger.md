# QA Preview Trigger

This file exists only to trigger a non-production Vercel **preview** deployment of the
`qa-app-slice` branch so the AppCalm slice can be browser-QA'd at mobile/desktop viewports.

- Branch: `qa-app-slice` (forked from `main` at `a501ea2`)
- Not for production. Production deploy remains owner-gated (`[deploy] [qa-approved]`).
- Safe to delete this file and branch after QA.
