# Threshold Rollout Tracker — the master backlog

Added 2026-07-12 (run 3), owner directive: **full transformation, greenfield** — not an indefinite trickle of one tiny page per day. This file is the single, concrete backlog every future session (scheduled or interactive) reads first and works down. It replaces "pick something small and safe" as the default planning method with "work the list, in tier order, in batches."

**Run 13 note (owner mandate shift, 2026-07-14 — READ THIS FIRST, supersedes the "re-skin, byte-identical" pattern below for remaining work):** the owner has since said, verbatim: "It's ideas, our job is to turn it into emotional warmth but modern, millennial and Gen Z, and funeral homes... I want this greenfield now, redo the whole thing, I'm not happy." This retires the presentation-only/byte-identical re-skin pattern (steps 2-3 below) as the default method for anything not yet shipped in Tier 1, and for all of Tier 2. Remaining pages get real IA rebuilds (new component structure, real content-density cuts, backend schema changes where the frontend needs them), not a token/color swap onto the same markup. See `docs/greenfield-rebuild-plan-2026-07-14.md` for the full rebuild scope, the explicit backend "what and why" schema-change list, and the new QR Transfer Pass module (family-controlled expiring-token intake handoff for funeral homes/hospice/cemetery/vendors — new Supabase tables, new frontend flows, PM-scoped in that doc, compliance-flagged copy pending review). The already-shipped Tier 0/Tier 1 re-skins below are not being thrown out — they're a real improvement over the pre-Threshold baseline and stay live — but they should not be treated as "done" in the sense of matching the wireframes/IA docs' actual density and structure; see `docs/redesign-diagnosis-2026-07-14.md` for the specific gap. `pages/funeral-home/dashboard.js` extraction (Tier 2 below) remains the single highest-priority engineering item, now reframed as extraction-plus-real-rebuild rather than extraction-plus-reskin.

**Run 12 note (owner-directed pause point, UPDATED with a bug finding):** after this run's depth-pass release, the owner asked to stop and review what's live against `docs/redesign/*.html` before more Tier 1/Tier 2 work ships. That still stands. But this run's post-deploy check ALSO found a real, reproducible React hydration error (#425/#418/#423) firing on every fresh load of `/trust`, `/mission`, and `/story` — see `docs/agent-operating-context-2026-07-14-run12.md` for full detail. This is a functional bug, not a design question, and should be picked up by a session with real shell/dev-build tooling **before** or **in parallel with** the owner's design review, not blocked behind it. Every prior "QA" pass on this tracker (including the one that shipped these three pages in run 9) has been the same agent instance reviewing its own diff/screenshots — this run is the first time a rigorous console-error check actually ran against these three pages, which is likely why this was never caught before.

**Read this file second, right after AGENTS.md's UX Redesign Directive, before `07-sprint-plan.md` or `11-funeral-home-polish-scope.md`** — those are still valid for narrative/rationale, but this file is the actual checklist and status source of truth. Update the checkboxes here every time a page ships. **As of run 9, also check the new Tier 0 section below first** — it covers the shared header/footer chrome and other foundation-level files that every other tier's pages render into, and which were not covered by any Tier 1/2/3 page-level batch.

**Owner directive, run 9 (continuation):** "after this I'd like full deploy and push of the revamped stuff and page and website redesign" — confirmed scope is the entire site, not just the funeral-home batch. Keep advancing this tracker in every future run (scheduled or interactive) until it is fully checked off, following the existing safe pattern (small reviewable batches, real QA, real post-deploy render checks, deploy budget respected) — not a blind rewrite of anything. **Superseded in method (not intent) by the run 13 note above: "full transformation" now means real rebuild, not re-skin.**

## Known issues (new section, run 12)

- **`/trust`, `/mission`, `/story` throw reproducible React hydration errors (#425/#418/#423) on every fresh page load**, found by run 12's post-deploy console check (first run to actually check console errors on these three pages rather than just visually/fetch-verify them). `/faq`, which shares the same `SiteChrome.js` chrome touched in run 12's depth-pass commit, loads clean — so this is not a blanket chrome regression. Run 12's own diff to these 3 files was a tiny (3-line) static `boxShadow` value addition with no structural/conditional changes, making it unlikely but not proven that run 12 introduced this; could not confirm against the pre-existing deployment due to the Vercel SSO wall. **Needs a session with real shell/dev-build tooling to reproduce with a non-minified build, find true root cause, and ship a verified hotfix.** Treat as higher priority than further Tier 1/Tier 2 design work.

## How to use this file each run

1. Find the first unshipped item in Tier 1. Take the next **2–5 items** (not just one) that are independent of each other — batch them into a single session's work, per AGENTS.md's "bundle two or three compatible small/medium fixes into one release candidate" rule, extended here to up to 5 for same-pattern presentation-only re-skins (or up to 8 when every item in the batch shares the exact same static/low-risk shape, as with this run's marketing-page tail).
2. Apply the exact proven-safe pattern from the `pages/funeral-home/summary.js` slice (2026-07-12, run 3): presentation-only re-skin onto Threshold tokens (`docs/redesign/08-visual-craft-standard.md` + `01-design-system-foundation.html`), zero changes to data-fetching/auth logic unless the page has none of its own real data to begin with (marketing/static pages), same component name and route. **Run 13: this presentation-only pattern is retired as the default for remaining/Tier 2 work — see the run 13 note above. Kept here as historical record of how the already-shipped Tier 0/1 items were done.**
3. **Run 7 caution:** if a page renders multiple conditional JSX branches through a shared helper function (e.g. `shell(content)` where `content` is a JSX literal built in each branch and passed in as an argument), do NOT use page-scoped `<style jsx>` for the shared/branch styles — babel-plugin-styled-jsx only applies its scope class to elements inside the helper's own directly-returned JSX tree, not to JSX passed in as an argument from a different return statement, so scoped selectors silently fail to match and the page renders unstyled. Use `<style jsx global>` for any styles that need to apply across content passed into a shared render-helper. Check any future page for this pattern (multi-branch state renders, e.g. loading/error/empty/success states through a shared shell) before shipping.
4. Prove the batch on a throwaway QA branch (temporarily drop `ignoreCommand` from `vercel.json`, restore it after), attempt interactive preview QA, but don't block on it if Vercel's SSO wall blocks preview access (documented, acceptable, recurring, and inconsistent limitation — confirmed again run 9, where some QA branch fetches got through and others on the same run did not). Note: a successful Vercel build does NOT catch styled-jsx scoping bugs like the run-7 one, and does NOT catch hydration mismatches like the run-12 finding — both are runtime/rendering issues, not compile errors. The live post-deploy render check (step 5) is what actually catches these, which is why it is mandatory and not skippable even when the build succeeds. **Run 12 addition: the render check must include an actual console-error read (not just a visual/fetch check) — the run-12 hydration bug on 3 pages went uncaught for multiple runs because earlier checks only verified visual rendering.**
5. Deploy as one batched `[deploy][qa-approved]` release. Run the live post-deploy render check (per `release-train.md`) on **every** page in the batch, at whatever viewport this session's browser tooling actually supports — record any tooling limitation honestly rather than skipping the check. **Also read browser console errors on every page in the batch, not just a visual check.** If the check finds a real rendering bug, ship a same-day hotfix commit and re-verify before considering the batch done — unless (as in run 12) the session lacks the tooling to safely diagnose and verify a fix, in which case document it clearly and flag it as the top priority for the next session with the right tooling, rather than guessing.
6. Check off every shipped item below, with the commit SHA and date, **in the same commit** that updates `docs/agent-operating-context.md` (or a dated addendum) — see the run 8/9 notes below on why this must be one commit, not a follow-up.
7. Only once **all of Tier 1 is checked off** does a run default to starting Tier 2 (monolith splits). Tier 2 items still get split into their own reviewable sub-batches per `11-funeral-home-polish-scope.md`'s method — never a blind full-file rewrite in one pass. **Exception, run 9:** the owner directed funeral-home-critical work to take priority over finishing the Tier 1 marketing-page tail first, then confirmed the goal is the full site — so both continue in parallel across runs, funeral-home first within each run's time budget. As of this batch, the plain-page portion of Tier 1's marketing/public list is fully shipped; the remaining Tier 1 items are all deliberately-deferred shared-chrome/shared-design-system pages (see below), each needing its own scoped slice rather than the batch pattern.
8. Tier 3 (internal/System Admin tooling) is lowest visual priority — functional correctness matters there, Threshold visual polish does not need to be first.
9. **Run 8 fix (verified working, run 9):** `scripts/check-agent-context.js`'s `contextTouched` check now also accepts dated addenda (`docs/agent-operating-context-YYYY-MM-DD-runN.md`) and this tracker file, not just the exact literal canonical filename. Confirmed by directly reading the live script on `main` in run 9, not just by re-citing this note. Fold the tracker + addendum update into the SAME commit as every release from now on — run 9 did this correctly for the SiteChrome release and both marketing batches, after partially missing it once earlier in the same run (see the run-9 addendum for the honest account).
10. **Run 12 addition:** before assuming you're the only session working this repo, check `list_deployments`/recent commit history for in-flight or very-recent activity from another scheduled instance — run 12 found a second, apparently concurrent instance push an overlapping release commit about 3 minutes after its own.

## Tier 0 — Shared chrome/foundation (discovered run 9 — blocks true visual consistency for every other tier)

Every page shipped in batches 3–8 re-skinned its own body content onto Threshold tokens, but none of them touched the shared components every page also renders. Until run 9, every one of those "shipped" pages still displayed pre-Threshold shared chrome around newly-tokened content — visually reading as two mismatched systems stacked on top of each other, which is the exact "two type systems collide" problem AGENTS.md's original current-state read flagged before the redesign started.

- [x] `components/SiteChrome.js` (`SiteHeader`, `SiteFooter`, `RoleActionStrip`, `SpineTrustStrip`, `StatusBadge`, `CHROME_COLORS`) — **SHIPPED 2026-07-13/14, commit `b6ed25f96c5597507b0c6536bcbe7df37a63e3d5`** (run 9). This is the shared header/footer chrome rendered by every page shipped in batches 3-8 (contact.js, faq.js, planning.js, packet.js, login.js, accept.js, confirm.js, participants.js, all vendors/* pages, funeral-home/summary.js/login.js/staff.js/setup.js/sample-case.js/workspace-demo.js) **and by `pages/funeral-home/dashboard.js` directly** (confirmed by reading both files — dashboard.js imports and renders `RoleActionStrip`, `SiteHeader`, `SiteFooter`, `SpineTrustStrip`). Presentation-only: no props, exports, auth-session-watch effect, admin-email gating, nav href list, or click handlers changed. Only `CHROME_COLORS` hex values (old cream/sage → Threshold bone/pine/clay, same key names), a few hardcoded state-badge hex literals remapped onto Threshold's clay family (waiting/needs-help) and pine family (handled/accepted) — still no siren-red per AGENTS.md — interactive nav/button elements bumped to full pill radius (999) per the visual-craft standard, and a local `CHROME_FONT_FAMILY` (Inter) constant used in place of the shared `lib/typography.js` `PASSAGE_FONT.family` (still Georgia) — deliberately scoped to this one file to avoid unknown blast radius into typography.js's other consumers. Verified via a throwaway QA branch build reaching READY on first attempt, a real fetched render of `/contact` on that branch, and the live post-deploy production render check (`X-Passage-Commit` confirmed matching on `www.thepassageapp.io/contact`).
- [x] Shadow/elevation depth pass (`lib/designSystem.js` `DS.shadow`, sticky glass header on `components/calm/CalmPublicChrome.js` + `components/SiteChrome.js`, hover-lift cards/buttons) — **SHIPPED run 12**. Addresses run 10's Q3 finding that shipped pages read correct but visually flat versus `docs/redesign/08-visual-craft-standard.md`'s layered-shadow/glass requirement. Presentation-only, same shadow-token key names so every existing consumer inherits automatically; `pages/mission.js`, `pages/story.js`, `pages/trust.js` also got a few missed card/button shadows added. Visually confirmed correct on production (sticky glass header, shadows, fonts all rendering). **But see "Known issues" above: this run's post-deploy check also found pre-existing-looking hydration errors on these same 3 marketing pages, not caused by this run's tiny diff as far as could be determined, but not fixed this run either.**
- [x] Site-wide Georgia/Inter font-token collision fix (`lib/typography.js` `PASSAGE_FONT.family`, `pages/_document.js` global Fraunces/Inter load) — **SHIPPED run 13, commit `3d881fde684fcc8cfdf5a828d2df87366364175a`**. Root-caused in `docs/redesign-diagnosis-2026-07-14.md`: `_app.js`'s global body style referenced `lib/typography.js`'s pre-Threshold Georgia constant, and `_document.js` never loaded Fraunces/Inter at all, so any page referencing those families without its own local `@import` silently fell back to system fonts. Fixed at the shared-token source rather than per-page. **Does NOT touch** the 156 hardcoded `Georgia,serif` instances in `dashboard.js` or the 7 in `urgent.js` (or `estate.js`'s `fontFamily: 'inherit'` pattern which now correctly inherits Inter) — those are separate, file-specific fixes still needed as part of each page's Tier 2 rebuild.
- [ ] Base `html body` styling (background `#f6f3ee`, color `#1a1916`) — **found, not yet fixed (run 9)**, font-family portion fixed run 13 (see above). Shared by `components/SiteChrome.js` (worked around locally this run — see above) and likely other consumers not yet audited. Low visible risk today since every shipped page wraps its content in its own explicitly-styled shell, so this only shows through in unstyled margins/gaps, not inside any card/layout — but it's the same "old default underneath" pattern `SiteChrome.js` had, so it should get the same treatment in a future batch rather than being forgotten.

## Tier 1 — small/medium pages, safe for aggressive batch re-skin (each <20KB, no monolith risk)

Funeral-home / operator surfaces (brief's "funeral-home demo path first" priority — clear these first within Tier 1):
- [x] `pages/funeral-home/summary.js` — **SHIPPED 2026-07-12, commit `027bd99a5ea6c25c74b1f28f1c219bd36fed007b`** (run 3)
- [x] `pages/funeral-home/login.js` — **SHIPPED 2026-07-12, commit `59c972f49a6a725056a15269d0066a03da603118`** (run 4). Static doorway/role-picker page, no data layer.
- [x] `pages/funeral-home/staff.js` — **SHIPPED 2026-07-12, commit `59c972f49a6a725056a15269d0066a03da603118`** (run 4). Magic-link + Google sign-in form; sendMagicLink/signInGoogle logic byte-identical.
- [ ] `pages/funeral-home/cases.js` — **checked, not a separate re-skin target (run 4)**: file is `export { default } from './dashboard'` — a pure re-export, no markup of its own. Tracked under the Tier 2 `dashboard.js` split instead; will inherit that work automatically once dashboard.js ships.
- [x] `pages/funeral-home/setup.js` — **SHIPPED 2026-07-12, commit `59c972f49a6a725056a15269d0066a03da603118`** (run 4). Director onboarding form incl. Supabase auth (getSession/onAuthStateChange/signInWithOtp) + `/api/partnerSelfServeSetup`; all state/effects/handlers byte-identical. `SmartAddressInput` component itself untouched — only the `colors`/`inputStyle` props passed to it changed to Threshold hex values.
- [x] `pages/funeral-home/sample-case.js` — **SHIPPED 2026-07-12, commit `59c972f49a6a725056a15269d0066a03da603118`** (run 4). Static sample-case marketing page; `trackEvent` calls preserved verbatim.
- [x] `pages/funeral-home/workspace-demo.js` (9.3KB) — **SHIPPED 2026-07-13, commit `77afae690df5c11c8825e6c09e100ad41dfdfcc3`** (run 5). Static sample-workspace marketing page; `trackEvent` calls preserved verbatim.
- [x] `pages/funeral-home/pilot-proof.js` — **inherited, no separate change needed (verified 2026-07-12, run 4)**: the file renders `FuneralHomeSampleCase` directly (a thin legacy-route wrapper that also does a `history.replaceState` to `/funeral-home/sample-case`), so it picked up the `sample-case.js` re-skin automatically. Confirmed live via the post-deploy render check.
- [x] `pages/funeral-home/index.js` (17.4KB) — **SHIPPED 2026-07-14, commit `9a874e808a8e439018e30387bd4e10eb2d0b1888`** (run 11). Resolved the run-6 shared-chrome concern by extending the `fhx-*` page-scoped override pattern fully instead of editing `components/calm/CalmPublicChrome.js` directly. Zero state/effect/handler/route/analytics changes. Live post-deploy render check on `/funeral-home` confirmed correct rendering (re-confirmed again run 12: sticky glass header renders, no console errors seen on this specific page this run).

Vendor portal:
- [x] `pages/vendors/index.js` (7.7KB) — **SHIPPED 2026-07-13, commit `77afae690df5c11c8825e6c09e100ad41dfdfcc3`** (run 5). Vendor front door; Supabase session watch (getSession/onAuthStateChange) + signOut + trackEvent calls byte-identical.
- [x] `pages/vendors/login.js` (7.6KB) — **SHIPPED 2026-07-13, commit `77afae690df5c11c8825e6c09e100ad41dfdfcc3`** (run 5). Vendor owner sign-in (Google + magic link); Supabase auth logic byte-identical.
- [x] `pages/vendors/accept.js` (7.7KB) — **SHIPPED 2026-07-13, commit `77afae690df5c11c8825e6c09e100ad41dfdfcc3`** (run 5). Vendor employee accept/sign-in with token/email query handling; Supabase auth + router.query logic byte-identical.
- [x] `pages/vendors/onboard.js` (11.4KB) — **SHIPPED 2026-07-13, commit `358fd0aa707379a1486926ad3a1bb620a5c532e2`** (run 6). Vendor application form; form state, update()/submit() handlers, and `/api/vendors/apply` contract byte-identical. `SmartAddressInput` component itself untouched — only `colors`/`inputStyle` props changed.
- [ ] `pages/vendors/request.js` (1KB) — **checked, not a separate re-skin target (run 5)**: it is a thin route wrapper that renders `components/vendor/VendorRequestApp.js` by default (token mode, auth dashboard mode, and `?demo=1` sample mode all live inside it); `LegacyVendorRequest` only renders behind an explicit `?legacy=1` QA fallback flag. So `VendorRequestApp.js` (not the legacy component) is the live code path and the correct future re-skin target — flagged as the next deferred Tier 1 item (run 10/11), on hold pending owner review per the run-12 note above.
- [x] `pages/vendors/admin.js` (17KB) — **SHIPPED 2026-07-13, commit `358fd0aa707379a1486926ad3a1bb620a5c532e2`** (run 6). System-admin vendor approval console; all state, the auth/session effect, load()/setStatus()/updateConnectDraft()/signIn()/signOut() functions, and the `/api/vendors/admin` GET/POST contracts byte-identical. `isSystemAdmin()` gate and `SYSTEM_ADMIN_EMAILS` unchanged.

Family / core auth and account:
- [x] `pages/login.js` (9.9KB) — **SHIPPED 2026-07-13, commit `0ab27a9c4eadd07ea87a505e7895fdd88aaf75fe`** (run 7). Main sign-in hub, 7 portal cards. Supabase OAuth-hash consumption, getSession/onAuthStateChange, admin routing logic, all trackEvent calls byte-identical.
- [x] `pages/accept.js` (9.9KB) — **SHIPPED 2026-07-13, commit `0ab27a9c4eadd07ea87a505e7895fdd88aaf75fe`** (run 7). Participant invite acceptance. router.query token handling, signIn()/sendMagicLink()/acceptInvite()/signOut(), /api/invitePreview + /api/acceptParticipantInvite contracts byte-identical.
- [x] `pages/confirm.js` (8KB) — **SHIPPED 2026-07-13, commit `0ab27a9c4eadd07ea87a505e7895fdd88aaf75fe`, hotfixed same day, commit `815ad94de9f8b61e1cc66c14a63d6420d58abb93`** (run 7). Death-confirmation multi-step flow. All state hooks and the /api/confirmTrigger contract byte-identical. **Post-deploy check caught a real bug:** the page's `shell(content)` helper pattern meant page-scoped `<style jsx>` never matched the content passed into it, so the page shipped completely unstyled to production for ~6 minutes before the hotfix. Fixed by switching to `<style jsx global>`. See the run-7 caution note added to the "How to use this file" section above — check future multi-branch-state pages for this same pattern before shipping.
- [x] `pages/participants.js` (7.2KB) — **SHIPPED 2026-07-13, commit `0ab27a9c4eadd07ea87a505e7895fdd88aaf75fe`** (run 7). Static participant-portal marketing page; trackEvent calls preserved verbatim.
- [ ] `pages/participating.js` (1.1KB) — **checked, not a separate re-skin target (run 7)**: same pattern as `pages/vendors/request.js` from run 5 — a thin route wrapper with an effect-based `?legacy=1` flag that renders `components/participant/ParticipantApp.js` by default (OTP sign-in, authed participantContext, multi-estate selector, invite-token acceptance, `?demo=1` sample mode all live inside it) or `components/participant/LegacyParticipating.js` behind the legacy flag. `ParticipantApp.js` (26.7KB) is the live code path and the correct future re-skin target — flagged as a deferred Tier 1 item (run 10/11), on hold pending owner review per the run-12 note above.
- [x] `pages/planning.js` (12.7KB) — **SHIPPED 2026-07-13, commit `586031920627b125a7b7c8faecae44753e464f95`** (run 8). Estate planning intake form. `emptyForm` (14 fields), `cleanEmail`, `Field`, localStorage draft (`passage_planning_draft`) + Supabase auth-session watch, `validate()`, `emailSecureLink()` (signInWithOtp), `savePlanningEstate()`/`/api/planningEstate` contract byte-identical. Single-return component (no shared render-helper) — not subject to the run-7 `shell(content)` styled-jsx risk. **Run 13 diagnosis finding: still a 12-field unbroken wall on one screen despite the Threshold tokens being correct — flagged for a real re-pace (2-3 field steps) as part of the greenfield rebuild, not just a visual issue.**
- [ ] `pages/today.js` (251 bytes — thin wrapper around `components/AppCalm.js`) — **checked, deliberately deferred (run 8)**: `AppCalm.js` routes between `LegacyApp`, `components/family/FamilyTodayApp.js`, and `Landing`, and imports the shared `lib/designSystem` tokens plus `components/calm/CalmControls` and `components/calm/CalmKit` — the same shared-design-system risk already flagged for `pages/funeral-home/index.js` (run 6, now resolved — see above). A naive re-skin here risks bleeding into every other consumer of those shared modules, so this needs its own PM-scoped slice rather than the proven single-page pattern. Not batched into run 8 for this reason. On hold pending owner review per the run-12 note above.
- [x] `pages/packet.js` (16.3KB) — **SHIPPED 2026-07-13, commit `5ddb597537a187954179abf60de1fdfbaa370127`** (run 8). Continuity-packet demo/download page. `buildContinuityPackets`/`demoContinuityInput`, `isPassageAdmin` admin-demo gate, the two packet-loading effects (`/api/estates/{id}/packets`), `copyActive()`/`packetStatus()`/`downloadActive()`, all trackEvent calls, and the `@media print` rules preserved byte-identical inside `<style jsx global>`. Checked for the run-7 `shell(content)` styled-jsx risk — not present (single return, no shared render-helper; 0 occurrences of `shell(` in the file).
- [x] `pages/contact.js` (9.9KB) — **SHIPPED 2026-07-13, commit `5ddb597537a187954179abf60de1fdfbaa370127`** (run 8). Support/sales contact form. `categories`, `Field`, URL query param parsing (category/plan), `submit()`/`/api/supportInquiry` contract, all trackEvent calls, `calendlyUrl`/`isMeetingCategory` byte-identical. Single-return component, no shared render-helper.
- [x] `pages/faq.js` (8KB) — **SHIPPED 2026-07-13, commit `5ddb597537a187954179abf60de1fdfbaa370127`** (run 8). Static FAQ page, 5 sections rendered through a `<details>`/`<summary>` accordion. No state/effects to preserve. Single-return component, no shared render-helper. **Re-verified run 12: loads clean with zero console errors on repeated fresh loads, unlike mission/story/trust (see Known issues above).**

Marketing / public (lower urgency than app surfaces, but still real live pages and easy wins — static content, no data-layer risk at all). **All items in this list are now SHIPPED as of run 9's second continuation batch — this closes out the full marketing/public list from the original brief:**
- [x] `pages/mission.js` (7.4KB) — **SHIPPED 2026-07-13/14, commit `f152b53f90ba316adbc73073a5a742f8674d44ef`** (run 9 continuation 1). Static mission/paths page. `useState`/`useEffect` auto-rotating path selector (6.5s interval, respects `prefers-reduced-motion`), all hrefs/copy byte-identical. Single-return component, no shared render-helper. Depth-pass shadow additions layered on in run 12 (see Tier 0). **Run 12 finding: throws reproducible hydration errors on every fresh load — see Known issues above.**
- [x] `pages/story.js` (7.8KB) — **SHIPPED 2026-07-13/14, commit `594fef635fc42736d7add1e7cca3a2aebbc6ad9f`** (run 9 continuation 1). Static founder-story page. `useState`/`useEffect` auto-rotating pane selector (same pattern as mission.js), all copy byte-identical. Uses a plain (non-styled-jsx) `<style suppressHydrationWarning>` template-literal block — not the styled-jsx risk pattern, no shared render-helper (0 occurrences of `shell(`). Depth-pass shadow additions layered on in run 12 (see Tier 0). **Run 12 finding: throws reproducible hydration errors on every fresh load, despite the page's own `suppressHydrationWarning` attribute on its style tag — see Known issues above. Worth noting for the fix session: `suppressHydrationWarning` only suppresses the warning for that one element's mismatch, not any mismatch elsewhere on the page, so this doesn't rule out the style tag as a cause.**
- [x] `pages/trust.js` (7.7KB) — **SHIPPED 2026-07-13/14, commit `594fef635fc42736d7add1e7cca3a2aebbc6ad9f`** (run 9 continuation 1). Static trust/compliance page. No state/effects. Single-return component, no shared render-helper. Depth-pass shadow additions layered on in run 12 (see Tier 0). **Run 12 finding: throws reproducible hydration errors on every fresh load — see Known issues above.**
- [x] `pages/privacy.js`, `pages/terms.js` — **SHIPPED this commit** (run 9 continuation 2). Static policy pages, no state/effects, single-return components, no shared render-helper.
- [x] `pages/resources.js` — **SHIPPED this commit** (run 9 continuation 2). `useState`/`useEffect` auto-rotating audience-pane selector (`titleCaseLabel`, `panes` array incl. spine/rules panes), all logic/hrefs byte-identical.
- [x] `pages/guides.js` — **SHIPPED this commit** (run 9 continuation 2). Real form state (email/name/interest/unlocked), `/api/supportInquiry` guide-lead-gate fetch, `passage_resource_lead_email` localStorage persistence — all preserved byte-identical.
- [x] `pages/pricing.js` — **SHIPPED this commit** (run 9 continuation 2). Highest-risk page in this batch: real Supabase auth (getSession/onAuthStateChange), `/api/checkout` Stripe flow, `passage_pending_pricing_checkout` localStorage replay effect, `trackEvent` calls throughout — all function bodies verified byte-identical, only style/color/font/radius values changed.
- [x] `pages/care-providers.js`, `pages/assisted-living.js` — **SHIPPED this commit** (run 9 continuation 2), via `components/CareProviderLanding.js` (both pages are thin wrappers passing a `focus` prop — confirmed by reading both page files). Real form state and `/api/careProviders/apply` submit handler preserved byte-identical.
- [x] `pages/hospice.js` — **SHIPPED this commit** (run 9 continuation 2). Largest page in the batch (warm-path care-prep record). Real Supabase auth, `/api/warmPathEstate` save flow, `SmartAddressInput` integration, `careMode` toggle state, `showDepth` progressive-disclosure state — all preserved byte-identical. The file's two `{false && <section>...}` dead JSX blocks (already unreachable in the original) were left in place untouched rather than removed, to keep this strictly a presentation diff.

## Tier 2 — large monoliths, split-then-reskin only, never a blind rewrite

**Run 13: "split-then-reskin" is retired as the end state for this tier — split, then actually rebuild each extracted piece per `docs/greenfield-rebuild-plan-2026-07-14.md`, not just re-token it.** Each of these needs its own PM-scoped punch list (see `11-funeral-home-polish-scope.md` as the template) before any code changes — extract into smaller components first, rebuild the extracted pieces for real (content density, IA, not just color/font), ship in reviewable slices. **As of run 9, this tier's `dashboard.js` item is the owner-directed top priority** (funeral-home portion is critical) — reconfirmed run 13.

- [ ] `pages/funeral-home/dashboard.js` — 449KB, live director/staff console. **Re-scoped with real structural findings, run 9** (superseding the size-only entry from run 2/`11-funeral-home-polish-scope.md`, which remains valid for persona-priority rationale). Confirmed run 13 via targeted grep (no full-file read available) that all 8 named components genuinely exist as top-level function declarations: `DirectorOperatingLoop`, `HelpOverlay`, `PartnerDirectorFocus`, `FocusWidget`, `PreparedOutputPreviewDialog`, `PartnerTaskActionDialog`, `PartnerAttentionInbox`, `ReportTable`. Also confirmed 156 hardcoded `Georgia,serif` instances and a duplicate stat-block bug (see `docs/qa-gap-assessment-2026-07-14.md`). Concrete extraction-first punch list for the next session with real code-execution tooling (a GitHub Actions-based extraction job is the recommended path — no bash/shell tooling has been available to any session so far for a file this size):
    1. Extract the 8 components above into their own files under `components/funeral-home/dashboard/` — mechanical move, zero behavior change, shrinks the monolith and makes later rebuilding reviewable per-component instead of per-449KB-file.
    2. Rebuild each extracted component for real onto Threshold tokens AND corrected content density (per the run-13 greenfield mandate, not a straight re-skin), starting with whatever is highest-visibility in a director's first 30 seconds (likely the root shell/case-list chrome, then `PartnerAttentionInbox`/`ReportTable`), and fixing the confirmed staff/director near-duplicate view along the way.
    3. QA each extracted batch with the real seeded orgs (Rivera Funeral Home (Demo), Test Funeral Home).
    4. Only after the shell + highest-visibility panes are rebuilt should staff-scope hiding (org-wide reporting/settings hidden from non-director employees) be layered in as its own reviewable slice.
- [ ] `pages/estate.js` — 313KB, family record surface. Run 13 diagnosis confirmed 5+ simultaneous panels visible on load. Real bug found run 13: literal `??` renders in place of a real value on `/estate` (e.g. "Waiting ?? 2 open") — fix this specific bug first, independent of the larger IA rebuild, since it's small and currently embarrassing on a live persona-facing page.
- [ ] `components/App.js` — 335KB (check what mounts this — likely the legacy family app shell; may be superseded by `components/family/FamilyTodayApp.js` + `AppCalm.js` already, verify before assuming it needs a rewrite vs. deletion)
- [ ] `pages/urgent.js` — 76.3KB. Run 13 diagnosis: the progressive-disclosure question flow itself is good and should be kept; needs the Threshold visual system (7 hardcoded Georgia instances found) and the siren-red "Minutes" card replaced with clay per AGENTS.md's no-siren-red rule.
- [ ] `pages/share.js` — 41KB
- [ ] `pages/announce.js` — 27.4KB
- [x] `components/participant/LegacyParticipating.js` / `components/participant/ParticipantApp.js` — **checked run 7**: `pages/participating.js` renders `ParticipantApp.js` (26.7KB) by default; `LegacyParticipating.js` (83.6KB) only renders behind an explicit `?legacy=1` QA flag. `ParticipantApp.js` is the live code path and the correct future re-skin target (tracked as a deferred Tier 1 item above).
- [ ] `components/vendor/LegacyVendorRequest.js` — 49.8KB — **confirmed dead code path by default (run 5):** `pages/vendors/request.js` only renders this component behind an explicit `?legacy=1` QA flag; `components/vendor/VendorRequestApp.js` (25.4KB) is the live default and the correct future re-skin target (tracked as a deferred Tier 1 item above).

## Tier 2.5 — New modules (run 13, greenfield mandate)

- [ ] **QR Transfer Pass** — family-controlled expiring-token intake handoff (hospice/funeral home/cemetery/vendors/estate admin). New Supabase tables (`transfer_pass_tokens`, `transfer_pass_consents`, decision pending on reusing/extending the existing dormant `provider_handoffs` table), new family-side + funeral-home-scan frontend flows. Full PM scope, acceptance criteria, and sourced compliance flags (HIPAA funeral-director exception, state-variable authority-to-act) in `docs/greenfield-rebuild-plan-2026-07-14.md`. **Compliance-sensitive copy is explicitly NOT cleared to ship** until reviewed per that doc's Section 5 — the feature/UX can be built now, exact legal-adjacent language cannot.

## Tier 3 — System Admin internal tooling (functional > visual, lowest priority for Threshold polish)

`pages/system/admin.js` (29.6KB) and `pages/system/admin/*.js`: `saas-roadmap.js` (48.5KB, the one true roadmap per AGENTS.md), `pilot-health.js`, `conversion-plan.js`, `funeral-home-qa.js`, `enterprise-funeral-home-readiness.js`, `automation-spine-readiness.js`, `rate-limit-readiness.js`, `partner-checkout-readiness.js`, `sprint-2.js`, plus `pages/system/demo.js`. These are internal-only, never persona-facing — leave on whatever styling works until Tier 1 and Tier 2 are substantially done. **Owner feedback, run 12: flagged as "still looks like shit" — accurate and expected given this deliberate deprioritization; will need its own scheduled pass once Tier 1/2 are further along.**

## Components that need a status check before Tier 1/2 work assumes they're the active code path

Several components have a "Legacy" naming pattern next to a newer "*App.js" component in the same directory (`components/participant/`, `components/vendor/`). Before spending a batch re-skinning either one, grep the actual `pages/*.js` files that import them to confirm which is live and which is dead code — do not re-skin dead code, and do not leave live legacy code unskinned because a newer-sounding file existed nearby. Record the finding here once checked.

`components/vendor/`: checked run 5 — `VendorRequestApp.js` is live by default, `LegacyVendorRequest.js` is dead code behind `?legacy=1`. `components/participant/`: checked run 7 — `ParticipantApp.js` is live by default, `LegacyParticipating.js` is dead code behind `?legacy=1`. Same pattern in both directories now confirmed.

## Pages that share chrome/design-system classes rather than owning isolated styling

`pages/funeral-home/index.js` **shipped run 11** (see Tier 1 above) — resolved via a fully-extended page-scoped override layer rather than editing the shared component. `pages/today.js` (via `components/AppCalm.js`) shares `lib/designSystem` tokens and `components/calm/CalmControls`/`CalmKit` with other consumers (checked run 8) — still open, on hold pending owner review per the run-12 note above. `components/SiteChrome.js` (Tier 0, above) was a third variant of this same problem class, now fixed. Re-skinning a shared-chrome page safely requires either (a) fully extending its own override layer so nothing shared visually changes (the pattern proven on `funeral-home/index.js`, run 11), or (b) a deliberate, explicitly-scoped decision to re-skin the shared design language itself, which would affect every other consumer too. Check for other pages using `CalmPublicChrome`, `lib/designSystem`, or similar shared-chrome patterns before batching them in future runs.

## Pages using a shared multi-branch render helper (styled-jsx scoping risk)

`pages/confirm.js` (run 7) used a `shell(content)` helper to wrap five different conditional JSX branches (loading/error/already/done/enter states). Its page-scoped `<style jsx>` block only had babel's scope class applied to elements inside `shell()`'s own directly-returned JSX (the `<main>`/`<div className="outer">` wrapper) — not to the branch content passed in as the `content` argument, since that JSX is authored in a separate return statement. This silently broke all styling in production (caught by the live post-deploy render check, not by the Vercel build, which succeeded normally) and required a same-day hotfix switching the scoped styles to `<style jsx global>`. Before re-skinning any future page with multiple conditional full-page states through a shared render helper, either use `<style jsx global>` for the shared styles, or restructure so the style tag lives inside the same JSX literal as the branch content it targets. **Checked run 8:** `pages/planning.js`, `pages/packet.js`, `pages/contact.js`, `pages/faq.js` are all single-return components with no shared render-helper (0 occurrences of `shell(` in each) — cleared of this risk. **Checked run 9:** `pages/funeral-home/dashboard.js`, `pages/mission.js`, `pages/story.js`, `pages/trust.js`, `pages/privacy.js`, `pages/terms.js`, `pages/resources.js`, `pages/guides.js`, `pages/pricing.js`, `components/CareProviderLanding.js`, `pages/hospice.js` also have 0 occurrences of `shell(` — cleared of this specific risk class. **Checked run 11:** `pages/funeral-home/index.js` is single-return, no shared render-helper — cleared. Remaining candidate to check before its own future re-skin: `components/family/FamilyTodayApp.js`. **Run 12 note: absence of the `shell()` pattern does NOT mean a page is free of hydration risk — see the new "Known issues" section above for a different class of bug (React hydration mismatch) found on 3 pages that are all clear of the shell() pattern.**

## Definition of "Tier 1 clear" (the milestone that unlocks Tier 2 by default)

Every checkbox in Tier 1 above is checked, with a shipped commit SHA next to it, and each one has passed a live post-deploy render check per `release-train.md`. At that point, update this file's header with the clear date and move the default next-action to `pages/funeral-home/dashboard.js` (Tier 2). **Run 9 update:** the full marketing/public plain-page list is now shipped, and the funeral-home operator/vendor/family-auth sub-lists were already clear as of runs 3-8. **Run 11 update:** `funeral-home/index.js` is now shipped, leaving three remaining deliberately-deferred shared-design-system pages (`today.js`/`AppCalm.js`, `vendors/request.js`→`VendorRequestApp.js`, `participating.js`→`ParticipantApp.js`) — each needs its own PM-scoped slice rather than the batch pattern, per their existing tracker notes. **Run 12 update: per the owner's explicit instruction, further Tier 1/Tier 2 work is paused pending an owner review of what's live against `docs/redesign/*.html`. This run also found a real hydration bug on 3 already-shipped pages (see "Known issues" above) that should be picked up ahead of or alongside that review by a session with real dev-build tooling. Do not resume batch shipping until the review happens, the owner says to continue, or the bug is fixed and verified. Run 13 update: the owner has since reviewed and responded — see the run 13 note at the top of this file. The pause is lifted; scope is now genuine greenfield rebuild, not resumed re-skinning.**
